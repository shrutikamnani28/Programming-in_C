Prog) Write a C program to read a square matrix of integers and compute its transpose.The transpose of a matrix is obtained by flipping it over its main diagonal, which means converting the rows into columns and columns into rows.
code:
#include <stdio.h>

int main() {
	int n;
	scanf("%d", &n);
	
	int matrix[n][n];

	
	//printf("Enter elements:\n");
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			scanf("%d", &matrix[i][j]);
		}
	}

	//printf("Transpose:\n");
	for (int i = 0; i < n; i++) {
		for(int j = 0; j < n; j++){
			printf("%d ", matrix[j][i]);
		}
		printf("\n");
	}
	return 0;
}


