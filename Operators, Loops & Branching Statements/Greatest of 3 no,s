Prog) Write a C program to display the greatest of three numbers using a conditional operator (ternary operator).
code:
#include <stdio.h>

int main() {
	int num1, num2, num3, greatest;


	printf("num1: ");
    scanf("%d", &num1);

	printf("num2: ");
    scanf("%d", &num2);

	printf("num3: ");
	
scanf("%d", &num3);

	greatest = (num1 > num2) ? ((num1 > num3) ? num1 : num3)
	                         : ((num2 > num3)? num2 : num3);

	printf("Greatest number: %d\n",greatest);

	return 0;
}
